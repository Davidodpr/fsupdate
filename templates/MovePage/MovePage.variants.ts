import { cva } from 'class-variance-authority'

export const movePageWrapperClasses = cva(['flex', 'flex-col', 'items-center', 'w-full', 'bg-[#E7ECF0]'])

export const todoItemWrapperClasses = cva(['w-full', 'max-w-4xl', 'mx-auto', 'px-4', 'py-2'])
