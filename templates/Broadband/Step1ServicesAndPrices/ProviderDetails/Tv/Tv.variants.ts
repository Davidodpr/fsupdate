import { cva } from 'class-variance-authority'

export const tvChannelsVariants = cva('my-4')

export const tvChannelIconsWrapperVariants = cva(['flex flex-wrap gap-2 justify-between items-center my-4', 'md:justify-start'])
